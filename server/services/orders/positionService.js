import Portfolio from "../../models/Portfolio.js";

export const getPositions = async (userId) => {
  // Returns: { symbol, quantity, averagePrice, currentPrice, marketValue, unrealizedPnL }
  return [];
};
